// put each element of array in one iteration function
const colors = ['red', 'green', 'black']

colors.forEach((color) => {
  console.log(color)
})

const numbers = [1, 2, 3, 4, 5]
let sum = 0

numbers.forEach((num) => {
  sum += num
})
console.log(sum)

